# pip resolves file: paths from CWD (project root), not from this file's location.
# Vercel runs pip from /vercel/path0/ (project root), so file:api and file:worker work.
sentinel-api @ file:api
sentinel-worker @ file:worker
# Remaining deps not declared in pyproject.toml files (pinned for Vercel):
uvicorn[standard]>=0.30,<1
asyncpg>=0.29,<1
aiosqlite>=0.20,<1
prometheus-client>=0.20,<1
bcrypt>=4,<5
pyotp>=2.9,<3
httpx>=0.27,<1
