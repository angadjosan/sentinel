Reasons it should exist as its own CLI:
1. Needs to run in CI
2. Needs its own set of tools - claude/codex harnesses are very restrictive. Need to be able to run process end to end and control it.

Can integrate Claude and Codex: make a skill on the marketplace for people to use. Skill + CLI (way better than MCP)
- Long vision: the labs RL us into their models

Two types of users for customer calls:
1. Vibecoders - these are the actual users. We need to make it as simple as possible for them to use. And show value - this is what could go wrong.
2. CISOs - they are going to tell us how to actually do cybersecurity.